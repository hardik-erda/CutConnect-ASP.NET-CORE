
@{
    ViewData["Title"] = "index";
}



<div>
    <div class="container mt-5">


        <div class="d-block w-75 m-auto">

            <h1 class="text-center ">Customer Dashboard</h1>
            <div class="d-flex align-items-center text-light text-end p-2 rounded  mt-5 bg-secondary" style="width: auto">

                <div class="me-auto">
                    <span class="float-start my-auto" id="SelectedCity" ></span>
                </div>

                <div class="ms-auto">

                    @* <asp:DropDownList class="align-middle" ID="CityList"  DataSourceID="SqlDataSource1" DataTextField="city" DataValueField="city">
                    </asp:DropDownList> *@
                    @* <asp:SqlDataSource ID="SqlDataSource1"  ConnectionString="<%$ ConnectionStrings:cutconnectConnectionString %>" ProviderName="<%$ ConnectionStrings:cutconnectConnectionString.ProviderName %>" SelectCommand="select distinct city from shop"></asp:SqlDataSource> *@

                    
                    <button class="btn btn-dark" ID="BtnFilterCity" OnClick="BtnFilterCity_Click">Filter City</button>
                </div>
            </div>
        </div>

        <div class="mt-3">
            <div class="text-center">
            <span class="text-light bg-danger p-1 px-3 rounded-2 h5 mt-5" ID="FailMessage" ></span>
            </div>
            <asp:Panel ID="ShopsInCityPanel" ></asp:Panel>
        </div>
    </div>
</div>